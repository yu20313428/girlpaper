<p id="notice"><%= notice %></p>

<p>
  <strong></strong>
  <%= @main.title %>
</p>

<p>
  <strong></strong>
  <%= @main.description %>
</p>

<p>
  <%= image_tag(@main.picture_url, width:600) if @main.picture.present? %>
</p>

<%= link_to 'Edit', edit_main_path(@main) %> |
<%= link_to 'Back', mains_path %>
